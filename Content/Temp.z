class Temp : ZilchComponent
{
    function Initialize(init : CogInitializer)
    {
        var sum = 0;
        for(var x = -1; x < 5; ++x)
        {
            for(var z = 3; z < 8; ++z)
            {
                sum += z;
            }
        }
        Console.WriteLine(sum);
    }
}
